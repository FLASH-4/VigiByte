# --- VIGIBYTE AI ENGINE DEPENDENCIES ---
# Purpose: High-performance libraries for the VigiByte Python Backend.
# Versions are pinned for compatibility with Render/Docker environments.

# --- WEB FRAMEWORK & SERVER ---
fastapi==0.115.0             # High-performance asynchronous API framework
uvicorn[standard]==0.30.6    # Production-ready ASGI server
python-multipart==0.0.9      # Form-data and Base64 stream processing

# --- CORE IMAGE & NUMERICAL LIBRARIES ---
Pillow==10.4.0               # Image decoding and PIL object management
numpy==1.26.4                # Vectorized operations for Cosine Similarity

# --- DEEP LEARNING & FACE RECOGNITION ---
deepface==0.0.93             # Wrapper for Facenet, RetinaFace, and VGG-Face
tf-keras==2.19.0             # Standalone Keras to ensure compatibility with TF 2.x
# Note: TensorFlow will be automatically fetched as a deepface dependency.